name: Hello world

on:
  push:
    branches:
      - main
      - dev

jobs:
  main-job:
    runs-on: ubuntu-latest

    steps:
      - name: Main branch message
        run: echo "Code pushed to main"
